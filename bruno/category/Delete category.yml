info:
  name: Delete category
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/categories/{{categoryId}}"
  auth:
    type: bearer
    token: "{{jwtToken}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        bru.setNextRequest('Delete highlight');

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

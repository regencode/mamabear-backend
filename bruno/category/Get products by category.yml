info:
  name: Get products by category
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/categories/{{categorySlug}}/product"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        bru.setNextRequest('Get user');

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

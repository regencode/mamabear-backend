info:
  name: "[ADMIN] Update Status"
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/admin/order/{{id}}/status"
  body:
    type: form-urlencoded
    data:
      - name: status
        value: PAYMENT_PAID
  auth: inherit

runtime:
  variables:
    - name: id
      value: 019eb26a-12fb-734c-944d-3c0ffcdd6bf3

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

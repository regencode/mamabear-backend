info:
  name: Delete review
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/api/reviews/{{reviewId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

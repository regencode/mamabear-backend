info:
  name: Bulk-Delete
  type: http
  seq: 21

http:
  method: DELETE
  url: "{{baseUrl}}/api/admin/products/bulk-delete"
  body:
    type: json
    data: |-
      {
        "ids":[1,2,3]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

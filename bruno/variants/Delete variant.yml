info:
  name: Delete variant
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/api/variants/{{variantId}}"
  auth:
    type: bearer
    token: "{{jwtToken}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        bru.setNextRequest('Delete product');

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

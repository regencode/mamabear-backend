info:
  name: Get user
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/users/{{userId}}"
  auth:
    type: bearer
    token: "{{jwtToken}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        bru.setNextRequest('Get highlight');

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

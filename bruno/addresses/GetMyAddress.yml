info:
  name: GetMyAddress
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/me/addresses/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

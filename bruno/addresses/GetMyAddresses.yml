info:
  name: GetMyAddresses
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/me/addresses"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Create payment
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/payment/create"
  body:
    type: json
    data: |-
      {
          "orderId": "ORDER-mjkolfdsjok",
          "subtotal": 100000,
          "customerDetails": {
              "firstName": "John",
              "lastName": "Doe",
              "email": "john@example.com",
              "phone": "081234567890"
          }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

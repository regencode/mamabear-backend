info:
  name: Verify email
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/auth/verify-email/{{token}}"
  auth: inherit

runtime:
  variables:
    - name: token
      value: verify-token-here

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
